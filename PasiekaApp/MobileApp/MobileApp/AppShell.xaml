<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MobileApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MobileApp"
    xmlns:pages="clr-namespace:MobileApp.Pages"
    Title="MobileApp">
    <!--Shell.FlyoutBehavior="Disabled"-->

    <!--<ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />-->

    <!-- Menu boczne (Flyout) -->
    <FlyoutItem Title="Navigation Bar">
        <ShellContent Title="Home" ContentTemplate="{DataTemplate pages:MainPage}" Route="MainPage" />
        <ShellContent Title="Tab1Page" ContentTemplate="{DataTemplate pages:Tab1Page}" Route="Tab1Page" />
        <ShellContent Title="ItemListPage" ContentTemplate="{DataTemplate pages:ItemListPage}" Route="ItemListPage" />
    </FlyoutItem>
    <!--<TabBar>
        <Tab Title="Home" Icon="home.png">
            <ShellContent ContentTemplate="{DataTemplate pages:MainPage}" Route="MainPage" />
        </Tab>
        <Tab Title="Page 1" Icon="page1.png">
            <ShellContent ContentTemplate="{DataTemplate pages:Tab1Page}" Route="Page1" />
        </Tab>
    </TabBar>-->
</Shell>
